# GUI Calculator App

File: `GUI _Calculator _app.py`

Description:
A GUI calculator application built with Tkinter. Filename contains spaces — use quotes when running.

Run:

```bash
python "GUI _Calculator _app.py"
```
