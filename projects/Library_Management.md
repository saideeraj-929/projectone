# Library Management

File: `Library_Management_Record.py`

Description:
Add, issue, and return books. Books are saved to `books.txt` and loaded on startup.

Run:

```bash
python "Library_Management_Record.py"
```

Screenshot:
See `Library Management screenshot.png` in the repository root.
