# Password Manager

File: `Password _maneger.py`

Description:
GUI app to generate and save passwords. Filename contains a space and a misspelling (`maneger`) — consider renaming to `password_manager.py`.

Run:

```bash
python "Password _maneger.py"
```
