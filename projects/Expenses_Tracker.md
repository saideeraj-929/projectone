# Expenses Tracker

File: `Expenses_Tracker.py`

Description:
Track expenses, delete entries and save/load expense data using a simple Tkinter GUI.

Run:

```bash
python "Expenses_Tracker.py"
```

Screenshot:
See `Expense Tracker screenshots` image in the repository root.
